local EnemyConfig = {}

EnemyConfig.Basic = {
    Health = 100,
    Speed = 8,
    Reward = 10
}

EnemyConfig.Fast = {
    Health = 50,
    Speed = 15,
    Reward = 15
}

EnemyConfig.Tank = {
    Health = 500,
    Speed = 4,
    Reward = 50
}

return EnemyConfig